services:
  storybook:
    container_name: storybook-raferdev
    environment:
      - ENV=PRODUCTION
    build: .
    volumes:
      - server_ngnix_vol:/app/storybook-static

volumes:
  server_ngnix_vol:
    external: true

networks: 
  default: 
    external: 
      name: portifolio