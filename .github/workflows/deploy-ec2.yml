on:
  push:
    branches:
      - main

name: Build and Deploy
jobs:
  build:
    runs-on: self-hosted
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Go path and Clear Docker
        run: cd packages/docs && ls && sudo docker-compose down && sudo docker system prune --all --force
      - name: Build
        run: ls && sudo docker-compose build
      - name: Run
        run: sudo docker-compose up -d
